import React from "react";
import PropTypes from "prop-types";
/*
 * This components creates a grid list which takes up 100% screen space
 * It is ideal for use on mobile screens
 *
 */

const GridList = ({ items }) => {
  return <p></p>;
};

GridList.propTypes = {
  items: PropTypes.string,
};

export default GridList;
